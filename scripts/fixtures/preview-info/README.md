# Native comment response fixtures

Reduced from public YouTube next responses for xUxN6tjZnws on 2026-09-12.
Only parser-relevant renderer/entity fields remain. Display names, comment
text, avatars, links, publication text and continuation tokens are replaced
with fixture values; tracking, request context and playback data are omitted.

The replies response contains three direct rows and three nested subthreads.
One deeper subthread has its own continuation; it must not become the page's
top-level continuation or cause already returned children to disappear.
